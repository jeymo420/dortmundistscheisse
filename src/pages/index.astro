---
import { capitalize } from "../utils/strings";
import Main from "../components/Main.astro";
import Layout from "../layouts/Layout.astro";
import Search from "../components/Search.astro";

const { target }: {
	target: string
} = Astro.locals;

const Component = {
	search: Search,
	"": Search,
	"www": Search,
}?.[target] as any || Main

---

<Layout title={target === "search" ? 'Suche' : `${capitalize(target)} ist scheiße`}>
	<Component />
</Layout>

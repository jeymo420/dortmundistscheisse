---
import { capitalize } from "../utils/strings";
import Main from "../components/Main.astro";
import Layout from "../layouts/Layout.astro";
import Search from "../components/Search.astro";

const {
	target,
}: {
	target: string;
} = Astro.locals;

const DEFAULT_TITLE = "Wer ist scheis.se?";

const targets = {
	"": { component: Search, title: DEFAULT_TITLE },
	search: { component: Search, title: DEFAULT_TITLE },
	www: { component: Search, title: DEFAULT_TITLE },
};

const Component = targets?.[target]?.component || Main;
const title = targets?.[target]?.title || `${capitalize(target)} ist scheiße`;
---

<Layout {title}>
	<Component />
</Layout>
